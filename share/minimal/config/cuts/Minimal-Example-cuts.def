# -*- mode: tqfolder -*-
# to get proper syntax highlighting and indentation when using emacs,
# add the following line to your .emacs file:
#   (load-file "$TQPATH/share/tqfolder.el")

+CutChannels {
    <.cutExpression = 1, .weightExpression = "[weight]", .title="Channel Selection">
    +CutMjj {
    	<.cutExpression = "[mjj] > 600000.", .title="$m_{jj} > 600$ GeV">
    } #End: CutVBFMjj
} #End: CutChannels